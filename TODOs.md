# TODOs

- Add a `paper-search` integration layer for article discovery:
  prefer API-first providers such as arXiv, Semantic Scholar, OpenAlex,
  and Zotero; use Browserbase only as a fallback for sites without usable
  APIs, dynamic conference pages, login/session flows, or PDF retrieval.
